<%= form_with(model: kit_product) do |form| %>
  <% if kit_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kit_product.errors.count, "error") %> prohibited this kit_product from being saved:</h2>

      <ul>
        <% kit_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
